<?xml version="1.0" encoding="utf-8"?>
<!-- One recording in a machine's pending-verdict queue. Chip-styled and
     ripple-backed so it reads as a control, not a line of text that happens
     to be clickable — the plain AlertDialog list this replaced looked the
     same whether or not a row could be tapped. -->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginBottom="8dp"
    android:background="@drawable/bg_chip"
    android:foreground="?attr/selectableItemBackground"
    android:clickable="true"
    android:focusable="true"
    android:orientation="horizontal"
    android:gravity="center_vertical"
    android:paddingHorizontal="16dp"
    android:paddingVertical="12dp">

    <LinearLayout
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_weight="1"
        android:orientation="vertical">

        <TextView
            android:id="@+id/pendingTimestamp"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:textColor="@color/injini_text"
            android:textSize="15sp"
            android:textStyle="bold" />

        <TextView
            android:id="@+id/pendingTier"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="2dp"
            android:textColor="@color/injini_dim"
            android:textSize="13sp" />
    </LinearLayout>

    <androidx.appcompat.widget.AppCompatImageView
        android:layout_width="18dp"
        android:layout_height="18dp"
        android:layout_marginStart="12dp"
        android:layerType="software"
        app:srcCompat="@drawable/ic_chevron_24"
        android:importantForAccessibility="no" />

</LinearLayout>
