// Top-level build file. Android Studio will likely prompt to update the
// Android Gradle Plugin / Kotlin versions on first open — accept those
// suggestions, this skeleton was never actually built/verified (no Android
// SDK available in the environment that generated it). See ../NEXT_STEPS.md.
plugins {
    id("com.android.application") version "8.6.0" apply false
    id("org.jetbrains.kotlin.android") version "2.0.20" apply false
}
