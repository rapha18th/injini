# Local-dev only. On the actual Space these are set as Repository secrets
# (Settings -> Variables and secrets), never committed, never in this file
# with real values.

HF_TOKEN=hf_xxx                          # write token, scoped to HF_DATASET_REPO only if possible
HF_DATASET_REPO=username/injini-field-data
UPLOAD_API_KEY=generate-with-openssl-rand--hex-24
ADMIN_USER=injini
ADMIN_PASSWORD=choose-a-real-password
